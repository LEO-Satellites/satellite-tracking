[configuration]
observatory = lasilla

year = 2021
month = 8
day = 31
delta = 60
window = evening

date = ${year}_${month}_${day}_${window}

[satellite]
satellite = oneweb
satellite_altitude_lower = 30
sun_zenith_lower = 97
sun_zenith_upper = 114

[directories]
work = .
output = ${work}/output
data_output = ${output}/output_files
tle = ${output}/tle_data

[names]
_tail = ${configuration:observatory}_${configuration:date}
tail = ${satellite:satellite}_${_tail}

simple = observing-details-${tail}
complete = visible-${tail}

// simple = observing-details-${satellite:satellite}_${output}
// complete = visible-${satellite:satellite}_${output}
